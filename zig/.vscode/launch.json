{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Main",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/bin/clock",
      "preLaunchTask": "build",
      "MIMode": "lldb",
      "cwd": "${workspaceFolder}"
    }
  ]
}
